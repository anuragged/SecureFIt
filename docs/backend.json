{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's profile and preferences within the SecureFit application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user profile."
        },
        "userId": {
          "type": "string",
          "description": "User ID from Firebase Authentication."
        },
        "email": {
          "type": "string",
          "description": "User's email address.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "User's first name."
        },
        "lastName": {
          "type": "string",
          "description": "User's last name."
        },
        "dateOfBirth": {
          "type": "string",
          "description": "User's date of birth.",
          "format": "date"
        },
        "gender": {
          "type": "string",
          "description": "User's gender."
        },
        "height": {
          "type": "number",
          "description": "User's height in centimeters."
        },
        "weight": {
          "type": "number",
          "description": "User's weight in kilograms."
        },
        "fitnessGoals": {
          "type": "string",
          "description": "User's fitness goals."
        }
      },
      "required": [
        "id",
        "userId",
        "email",
        "firstName",
        "lastName"
      ]
    },
    "WorkoutSession": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "WorkoutSession",
      "type": "object",
      "description": "Represents a workout session logged by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the workout session."
        },
        "userId": {
          "type": "string",
          "description": "Reference to UserProfile. (Relationship: UserProfile 1:N WorkoutSession)"
        },
        "startTime": {
          "type": "string",
          "description": "Start time of the workout session.",
          "format": "date-time"
        },
        "endTime": {
          "type": "string",
          "description": "End time of the workout session.",
          "format": "date-time"
        },
        "activityType": {
          "type": "string",
          "description": "Type of workout activity (e.g., running, swimming, weightlifting)."
        },
        "duration": {
          "type": "number",
          "description": "Duration of the workout session in minutes."
        },
        "caloriesBurned": {
          "type": "number",
          "description": "Estimated calories burned during the workout session."
        },
        "distance": {
          "type": "number",
          "description": "Distance covered during the workout session (if applicable)."
        },
        "notes": {
          "type": "string",
          "description": "Optional notes about the workout session."
        }
      },
      "required": [
        "id",
        "userId",
        "startTime",
        "endTime",
        "activityType",
        "duration",
        "caloriesBurned"
      ]
    },
    "FitnessMetric": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "FitnessMetric",
      "type": "object",
      "description": "Represents a specific fitness metric recorded for a user at a particular time.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the fitness metric record."
        },
        "userId": {
          "type": "string",
          "description": "Reference to UserProfile. (Relationship: UserProfile 1:N FitnessMetric)"
        },
        "timestamp": {
          "type": "string",
          "description": "Timestamp when the metric was recorded.",
          "format": "date-time"
        },
        "metricType": {
          "type": "string",
          "description": "Type of fitness metric (e.g., heart rate, steps, sleep duration)."
        },
        "metricValue": {
          "type": "number",
          "description": "Value of the fitness metric."
        },
        "unit": {
          "type": "string",
          "description": "Unit of measurement for the metric (e.g., bpm, steps, hours)."
        }
      },
      "required": [
        "id",
        "userId",
        "timestamp",
        "metricType",
        "metricValue",
        "unit"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores user profile data. Access is restricted to the user identified by userId."
    },
    "/users/{userId}/workoutSessions/{workoutSessionId}": {
      "schema": {
        "$ref": "#/entities/WorkoutSession"
      },
      "description": "Stores workout session logs for a specific user. Access is restricted to the user identified by userId."
    },
    "/users/{userId}/fitnessMetrics/{fitnessMetricId}": {
      "schema": {
        "$ref": "#/entities/FitnessMetric"
      },
      "description": "Stores fitness metrics recorded for a specific user. Access is restricted to the user identified by userId."
    }
  }
}
